# Single-container setup: FastAPI serves both API and static frontend
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: insightai
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8000
    restart: unless-stopped
