# Render Blueprint - InsightAI Deployment
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: insightai-ml-platform
    env: docker
    plan: free
    healthCheckPath: /ping
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
